{
  "name": "paxlab-subs",
  "private": true,
  "version": "0.0.1-dev0.1",
  "type": "module",
  "description": "Prototype leger PAXLAB Subs - lecteur audio + sous-titres + surlignage mot actif.",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/build.mjs",
    "preview": "npx http-server dist -p 5173 -c-1"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
